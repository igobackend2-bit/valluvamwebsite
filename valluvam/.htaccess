# SECURITY FIX (23 Sep 2026): this folder is an old duplicate copy of the
# whole site (including its own .env). It must never be served publicly.
# (The previous rules here forced https/www and caused an endless redirect loop.)
Require all denied
